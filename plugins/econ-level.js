{algoritma yang akan digunakan untuk diskon sebuah toko}
   deklarasi
       harga       : integer
       diskon      : integer
       total_belanja: integer
   algoritma
       read (diskon) {20%}
       read (harga)
       if nilai >= 200.000 then
              total_belanja <== 'diskon
   